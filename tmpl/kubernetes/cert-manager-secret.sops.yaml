---
apiVersion: v1
kind: Secret
metadata:
  name: cert-manager-secret
  namespace: cert-manager
stringData:
  cloudflare_api_key: "${BOOTSTRAP_CLOUDFLARE_APIKEY}"
  zerossl_eab_key: "${BOOTSTRAP_ZEROSSL_EAB_KEY}"
